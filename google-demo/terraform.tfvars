# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored by convention.
#
# Passwords are NOT set here — see .env.example. Terraform auto-reads
# any TF_VAR_<name> environment variable as the value for variable "<name>".

project_id  = "second-abacus-508007-i8"
region      = "us-central1"
bq_location = "US"
